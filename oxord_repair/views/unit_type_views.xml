<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- List View -->
        <record id="view_repair_unit_type_list" model="ir.ui.view">
            <field name="name">repair.unit.type.list</field>
            <field name="model">repair.unit.type</field>
            <field name="arch" type="xml">
                <list string="Unit Types">
                    <field name="name"/>
                    <field name="department_id"/>
                    <field name="active"/>
                </list>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_repair_unit_type_form" model="ir.ui.view">
            <field name="name">repair.unit.type.form</field>
            <field name="model">repair.unit.type</field>
            <field name="arch" type="xml">
                <form string="Unit Type">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="department_id"/>
                            <field name="active"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_repair_unit_type" model="ir.actions.act_window">
            <field name="name">Unit Types</field>
            <field name="res_model">repair.unit.type</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to add a new Unit Type.
                </p>
            </field>
        </record>

    </data>
</odoo>
