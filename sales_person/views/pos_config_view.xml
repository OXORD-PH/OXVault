<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_config_form_inherit_sales_person" model="ir.ui.view">
        <field name="name">pos.config.form.inherit.sales.person</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">

            <!-- Insert into POS Features group -->
            <xpath expr="//group[@name='pos_features']" position="inside">
                <field name="module_sales_person"/>
            </xpath>

            <!-- Your existing Allowed Sales Person group -->
            <xpath expr="//sheet" position="inside">
                <group string="Allowed Sales Person">
                    <field name="sales_person_ids" widget="many2many_tags"/>
                </group>
            </xpath>

        </field>
    </record>
</odoo>
